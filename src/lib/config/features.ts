export const features = {
  // Toggle the KYC (Know Your Customer) module.
  // Set to true to require KYC verification and show it in the UI.
  // Set to false to disable it entirely (e.g. for demo purposes).
  KYC_ENABLED: false,

  // Toggle Twilio SMS Notifications.
  SMS_ENABLED: false,

  // Toggle Email Notifications (Nodemailer).
  EMAIL_ENABLED: true,
};
